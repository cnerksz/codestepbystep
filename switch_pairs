def switch_pairs(a):
    b=[]
    str=""
    for j in range(1,len(a),2):
        b.append(a[j])
    for i in range(0,len(a),2):
        b.insert(i+1,a[i])
    for t in range(0,len(b)):
        str=str+b[t]
    return str



