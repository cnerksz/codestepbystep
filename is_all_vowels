def is_all_vowels(a):
    count=0
    b="aeiou"
    t=len(a)
    for i in range(len(a)):
        if a[i]=="_":
            t-=1
        if a[i] in b or a[i] in b.upper():
            count+=1
    if count==t:
        return True
    else:
        return False
    
